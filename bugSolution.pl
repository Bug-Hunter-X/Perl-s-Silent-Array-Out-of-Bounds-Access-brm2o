my @array = (1, 2, 3); my $index = 3; if ($index < @array) { my $element = $array[$index]; # Access element only if index is valid print "Element at index $index: $element\n"; } else { print "Index $index is out of bounds\n"; } #Alternative using defined to check if undef my @array = (1,2,3); my $element = $array[3]; if (defined $element){ print "Element is: $element\n";} else { print "Element is undef\n";}